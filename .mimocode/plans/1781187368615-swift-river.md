# Plan: Complete All Function Interfaces for Test Coverage

## Goal
Add function/class interfaces (signatures + placeholder bodies) for all functions that the test suite requires, organized according to the existing architecture. No implementation logic — just clean interfaces that you can fill in yourself.

## Current Status
- `model.py`: 7 classes implemented (Embedding, Linear, RMSNorm, Swiglu, Softmax, RoPE, Scaled_dot_product_attention). **Bug**: `Scaled_dot_product_attention.forward()` references `self.dim` which is never defined.
- `data.py`: `run_get_batch` implemented.
- `nn.py`: **Empty file**.
- `optim.py`: **Does not exist**.
- `tokenizer.py`: **Does not exist**.
- 14 adapter functions still raise `NotImplementedError`.

## Files to Modify/Create

### 1. `cs336_basics/model.py` — Add 2 classes

**`MultiheadSelfAttention`** (no RoPE)
- `__init__(self, d_model: int, num_heads: int)`
- `forward(self, in_features: Tensor, token_positions: Tensor | None = None) -> Tensor`
- Uses internal `Linear` layers for Q/K/V/O projections

**`MultiheadSelfAttentionWithRoPE`** (with RoPE)
- `__init__(self, d_model: int, num_heads: int, max_seq_len: int, theta: float)`
- `forward(self, in_features: Tensor, token_positions: Tensor | None = None) -> Tensor`

**`TransformerBlock`**
- `__init__(self, d_model: int, num_heads: int, d_ff: int, max_seq_len: int, theta: float)`
- `forward(self, in_features: Tensor) -> Tensor`
- Pre-norm architecture: RMSNorm → MHA → residual → RMSNorm → SwiGLU → residual

**`TransformerLM`**
- `__init__(self, vocab_size: int, context_length: int, d_model: int, num_layers: int, num_heads: int, d_ff: int, rope_theta: float)`
- `forward(self, in_indices: Tensor) -> Tensor`
- Token embeddings → N TransformerBlocks → final RMSNorm → LM head (Linear)

**Fix**: `Scaled_dot_product_attention.__init__` needs `self.dim` set, or `forward` should use hardcoded `-1`.

### 2. `cs336_basics/nn.py` — Create with 5 functions/classes

- `def silu(x: Tensor) -> Tensor` — SiLU activation
- `def softmax(x: Tensor, dim: int) -> Tensor` — numerically stable softmax
- `def cross_entropy(inputs: Tensor, targets: Tensor) -> Tensor` — average cross-entropy loss
- `class ScaledDotProductAttention` — if not staying in model.py, put here

### 3. `cs336_basics/optim.py` — Create with 3 items

- `class AdamW(torch.optim.Optimizer)` — with `step()` method
- `def get_lr_cosine_schedule(it, max_lr, min_lr, warmup_iters, cosine_cycle_iters) -> float`
- `def gradient_clipping(parameters: Iterable[Parameter], max_l2_norm: float) -> None`

### 4. `cs336_basics/tokenizer.py` — Create with 2 items

- `class Tokenizer` — with `encode(text)`, `decode(ids)`, `encode_iterable(iterable)` methods
- `def train_bpe(input_path, vocab_size, special_tokens, **kwargs) -> tuple[dict[int, bytes], list[tuple[bytes, bytes]]]`

### 5. `cs336_basics/checkpoint.py` — Create with 2 functions

- `def save_checkpoint(model, optimizer, iteration, out) -> None`
- `def load_checkpoint(src, model, optimizer) -> int`

### 6. `tests/adapters.py` — Wire all adapters to new interfaces

Replace each `raise NotImplementedError` with a thin call to the corresponding `cs336_basics` implementation.

## Execution Order

1. Fix `Scaled_dot_product_attention` bug in `model.py`
2. Add `MultiheadSelfAttention`, `MultiheadSelfAttentionWithRoPE`, `TransformerBlock`, `TransformerLM` to `model.py`
3. Create `nn.py` with `silu`, `softmax`, `cross_entropy`
4. Create `optim.py` with `AdamW`, `get_lr_cosine_schedule`, `gradient_clipping`
5. Create `tokenizer.py` with `Tokenizer` class and `train_bpe`
6. Create `checkpoint.py` with `save_checkpoint`, `load_checkpoint`
7. Wire all adapters in `tests/adapters.py`
8. Update `__init__.py` to export new modules

## Verification
Run `uv run pytest tests/test_model.py tests/test_nn_utils.py tests/test_optimizer.py tests/test_serialization.py tests/test_tokenizer.py tests/test_train_bpe.py` — tests will fail on implementation correctness (expected since we only write interfaces), but they should at least import without errors and reach the assertion/NotImplementedError points without import crashes.
